{
  "id": "ssh",
  "name": "SSH Plugin",
  "version": "0.1.0",
  "plugin_api_version": "1.0",
  "entrypoint": "plugins.ssh.plugin:Plugin",
  "description": "Run configured SSH commands and expose them through a slash command and tool.",
  "required_config_keys": ["default_host", "connect_timeout_seconds", "command_timeout_seconds"],
  "permissions": ["messaging"],
  "enabled": true
}
